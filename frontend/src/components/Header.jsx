import React from "react";
import styled from "styled-components";

const Head = styled.div`
  padding: 20px;
  background: #665fee;
  color: white;
  font-size: 24px;
`;

const Header = () => <Head>Medical Appointment Assistant</Head>;

export default Header;
